import React from 'react'

const UserFullName = ({ userFullName }) => {
    return (
        <div className="mt-px md:mt-0.5 drop-shadow white-text opacity-80 text-lg">{userFullName}</div>
    )
}

export default UserFullName